# Инструкции для Claude Code

## Python
- Всегда используй venv для установки и запуска: `venv/bin/pip install` и `venv/bin/python script.py`
- Никогда не устанавливай в системный Python

Старайся использовать python не старше 3.12
/opt/homebrew/opt/python@3.12/bin/python3.12 -m venv venv

## arch
не создавай лишних .md файлов если не просят

актуализируй .gitignore чтобы не пушить лишние артефакты